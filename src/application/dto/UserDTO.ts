/**
 * User DTO (Data Transfer Object)
 *
 * This type is automatically inferred from the Zod schema.
 * See userDTOSchemas.ts for the schema definition.
 *
 * Benefits of using Zod schema inference:
 * - Single source of truth (no duplicate definitions)
 * - Runtime validation capabilities
 * - Automatic type safety
 */
export { UserDTO, userDTOSchema } from './userDTOSchemas';
